# Update Website Domain and SEO Metadata

Update the website's primary domain to `olawalerefrigeration.co.za` and synchronize SEO metadata to match the company branding.

## 1. Update `package.json`
- Add `"homepage": "https://olawalerefrigeration.co.za"` to define the production URL.

## 2. Update `index.html`
- Update `<title>` to "Olawale Refrigeration | Fridge & Aircon Repair Johannesburg".
- Update meta `description` and `keywords` for better SEO.
- Replace all "Gebeya Dala" placeholders with "Olawale Refrigeration".
- Update `og:url` and `twitter:url` to `https://olawalerefrigeration.co.za`.
- Update Social Media (OG/Twitter) tags with professional descriptions and titles.

## 3. Verification
- Validate the build to ensure the changes are correctly integrated.
- Verify that the homepage URL is correctly set in configuration.
